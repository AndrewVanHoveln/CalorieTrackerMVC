@model List<Calorie_Tracking_App.Models.Food>
@{
    ViewData["Title"] = "Tracker";
}

<h2>Today's Entries</h2>

<table id="EntryTable">
    <thead>
        <tr>
            <th style="display:none;">Id</th>
            <th>Name</th>
            <th>Carbs (g)</th>
            <th>Fats (g)</th>
            <th>Protien (g)</th>
            <th>Calories (kcal)</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Count > 0){
            foreach(var food in Model){
                <tr>
                    <td>@food.Name</td>
                    <td>@food.Carbs</td>
                    <td>@food.Fats</td>
                    <td>@food.Protien</td>
                    <td>@food.Calories</td>
                    <td class= "bg-dark" style="border: none; padding-left: 20px; color: white;">
                        <i class="fa-solid fa-trash-can fa-2x" onclick="deleteFood(@food.Id)" style="cursor: pointer;"></i>
                    </td>
                </tr>
            }
        }
        <tr>
            <form method="post" asp-action="Add">
                <td><input type="text" name="Name" required /></td>
                <td><input type="number" name="Carbs" step="0.01" min="0" required/></td>
                <td><input type="number" name="Fats" step="0.01" min="0" required/></td>
                <td><input type="number" name="Protien" step="0.01" min="0" required/></td>
                <td><input type="number" name="Calories" min="0" required/></td>
                <td style="border: none; padding-left: 20px;"><input type="submit" value="Add"/></td>
            </form>
        </tr>
    </tbody>
</table>

@section Scripts{
    <script>
        function deleteFood(id){
            fetch(`/CalorieTracker/Delete/${id}`, {
                method: 'DELETE',
                headers: {
                    'Content-Type': 'application/json'
                }
            }).then(response => {
                if(response.ok){
                    location.reload();
                } else{
                    alert('Failed to delete')
                }
            }).catch(Error => console.error(error));
        }
    </script>
}


